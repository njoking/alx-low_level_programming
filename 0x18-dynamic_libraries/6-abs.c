#include "main.h"
/**
 * _abs - prints the absolute value of the character
 * @k: this is the character in questionn
 * Return: (-k) or (k)
 */
int _abs(int k)
{
	if (k < 0)
		return (-k);
	else
		return (k);
}
