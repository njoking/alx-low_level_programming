#include "main.h"
/**
 *  _sqrt_recursion -  returns the natural square root
 *  @n: character in question
 *  Return: square root
 */
int _sqrt_recursion(int n)
{
	if (n * n == n)
		return (b);
	else if (n * n > 0)
		return (-1);
	return (_sqrt_recursion(n, n + 1));
}
