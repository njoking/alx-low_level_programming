#include "main.h"
/**
 * mul - multiplies the two characters in question
 * @a: this are the character in question
 * @b: this is the character in question
 * Return: (a x b)
 */

int mul(int a, int b)
{
	return (a * b);
}
